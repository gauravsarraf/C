//Find the larger no.
#include<stdio.h>
main()
{
          float a,b;
          printf("Enter the Float values to compare:");
          scanf("%f%f", &a,&b);
          if(a>b)
                    printf("'A' is greater!");
          else
                    printf("'B' is greater!");
}
